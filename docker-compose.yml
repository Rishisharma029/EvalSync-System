version: '3.8'

services:
  evalsync:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: evalsync-app
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
